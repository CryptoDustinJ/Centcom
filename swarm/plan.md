# Swarm Plan: Fix Star Office UI

## Agents
1. **Debugger** - Finds runtime errors, adds error reporting, checks browser console
2. **Integrator** - Fixes agent state integration, ensures OpenClaw agents push updates
3. **Artist** - Verifies assets, LAYOUT data, and rendering pipeline

## Approach
- Parallel work on different aspects
- Each agent documents findings in `swarm/`
- Combine fixes and test
